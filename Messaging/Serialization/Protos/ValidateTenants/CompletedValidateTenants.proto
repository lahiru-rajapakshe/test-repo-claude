
syntax = "proto3";
import "google/protobuf/timestamp.proto"; // Import timestamp for date/time fields

message CompletedValidateTenants {
  repeated TenantValidationResult validation_results = 1;

  message TenantValidationResult {
    string tenant_id = 1;
    bool valid = 2;
    string error_message = 3;
  }
}